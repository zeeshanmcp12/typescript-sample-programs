function makeShirt(size, message) {
    console.log("Size of the shirt is \"".concat(size, "\" and the message to be printed on it is \"").concat(message, "\"."));
}
makeShirt("Small", "I write code!");
